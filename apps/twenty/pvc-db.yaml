apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-pvc
  namespace: twentycrm
spec:
  storageClassName: nfs-client
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
